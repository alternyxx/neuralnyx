use neuralnyx::*;

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_modulo() {
        let mut inputs: Vec<Vec<f32>> = Vec::new();
        let mut outputs: Vec<Vec<f32>> = Vec::new();
        for i in (0..3).step_by(2) {

        }


        // let mut nn = NeuralNet::new(&mut input, &mut outputs);
    }
}